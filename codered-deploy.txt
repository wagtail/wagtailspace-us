# This is an SFTP batch file used for copying files from this repo up to
# CodeRed cloud. Include any files or folders here which should be copied
# to the server.
#
# SFTP credentials are accessible from https://app.codered.cloud/
#
# USAGE:
#     cat codered-deploy-sftp.txt | sftp wagtailspace-us@wagtailspace-us.codered.cloud
#

put -r wagtailspace /www/wagtailspace
put manage.py /www/
put requirements.txt /www/
put config-prd-stats.json /www/
